{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "agencies.schema.json",
  "title": "Agencies Configuration",
  "description": "A national government office physically present in the city (e.g. PNP, BFP, NBI, BJMP, Prosecutor). Office-shaped (name, location, contact) but national, not a city department, so it is deliberately kept out of offices.json/OfficeGroup (see ADR-0004). Referenced by services[].providedByAgency.",
  "type": "object",
  "required": ["agencies"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference"
    },
    "agencies": {
      "type": "array",
      "description": "All Agency records",
      "items": { "$ref": "#/definitions/agency" }
    }
  },
  "definitions": {
    "agency": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name", "icon", "description"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique, URL-friendly Agency identifier (referenced by services[].providedByAgency)"
        },
        "name": { "type": "string" },
        "icon": {
          "type": "string",
          "description": "Bootstrap icon class"
        },
        "description": { "type": "string" },
        "location": { "type": "string" },
        "phone": { "type": "string" },
        "hours": { "type": "string" },
        "sources": {
          "type": "array",
          "description": "Documents this Agency's contact/location data was transcribed from, primary first (#243, ADR-0005). Replaces the sourceUrl/sourceName pair and the dataStatus enum: confidence is derived from provenance, not asserted by a field.",
          "items": { "$ref": "#/definitions/sourceRef" }
        }
      }
    },
    "sourceRef": {
      "type": "object",
      "additionalProperties": false,
      "description": "One document a record was transcribed from (#243, ADR-0005). Only `name` is required: the validator deliberately enforces nothing else about provenance (#237) — the Office page render, not validation, is what makes an unsourced verification claim unreachable.",
      "required": ["name"],
      "properties": {
        "name": {
          "type": "string",
          "description": "Issuing authority and document, e.g. \"Las Piñas City Civil Registry Citizen's Charter, 2024 edition\". Names the city, never this site."
        },
        "url": {
          "type": "string",
          "description": "Link to the document, present only when the authority publishes it. Absent for a tarpaulin or a PDF handed over on request; its absence never makes a record unverified (#238). Never a contributor's own photo or scan."
        },
        "published": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Vintage of the source itself, e.g. '2022'. Kept separate from verifiedOn so a fresh check cannot present old data as current. Nothing computes from it — there is no stale threshold (#237)."
        },
        "verifiedOn": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "ISO date this record was checked against THIS source. Asserts transcription accuracy, not currency."
        },
        "covers": {
          "type": "array",
          "description": "Field names this source backs, e.g. [\"phone\"]. Omitted for the source backing the record generally. Stored for auditability, not rendered.",
          "items": { "type": "string" }
        }
      }
    }
  }
}
